@model Student
@{
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    @Model.Id @Model.Name
    <img src="~/Images/@Model.StdImg" class="img-fluid" />
    <a asp-action="Download" asp-route-id="@Model.Id" class="btn btn-success">Download Img</a>
</body>
</html>
